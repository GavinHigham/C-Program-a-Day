#import <stdio.h>

//Prints the size of various C types. I'll periodically add more types as I need to know them.

int main() {
	printf("Size of a long long int: %lu\n", sizeof(long long unsigned int));
	return 0;
}